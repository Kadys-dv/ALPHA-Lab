"use client";
import { useEffect,useState } from "react";
export type MetricValue=number|null;
export type PublicStatusState="loading"|"ok"|"partial"|"unavailable";
export type Freshness={checkedAt:string;ageSeconds:number;maxAgeSeconds:number;isFresh:boolean;source?:string};
export type PublicStatus={state:PublicStatusState;checkedAt:string|null;requestId?:string;freshness?:Freshness;snapshot?:{checkedAt:string;state:PublicStatusState;metrics:PublicStatus["metrics"];freshness:Freshness};source?:{provider:string;repository:string;healthySources:number;totalSources:number};metrics:{submitted:MetricValue;underReview:MetricValue;accepted:MetricValue;approvalRate:MetricValue;completionRate:MetricValue;started:MetricValue;dropoffs:MetricValue;uniqueBuilders:MetricValue;distinctProjects:MetricValue;repeatBuilders:MetricValue;repeatProjects:MetricValue;acceptedPerBuilder:MetricValue;averageReviewHours:MetricValue;medianReviewHours:MetricValue;p90ReviewHours:MetricValue;feedbackCount:MetricValue;privateFeedbackRequests:MetricValue;usefulRate:MetricValue;appliedRate:MetricValue;repeatIntentRate:MetricValue;willingnessToPay:MetricValue};rubric?:Record<string,{approved:number;adjustments:number;measured:number}>;rubricVersion?:number;targets?:{participants:number;submissions:number;repeatBuilders:number;willingnessToPay:number;reviewSlaHours:number};collections?:{builders:{visible:number;total:number|null;limit:number};startedCohort:{visible:number;total:number|null;limit:number};reviewQueue:{visible:number;total:number|null;limit:number}};startedCohort?:Array<{issue:number;author:string;repository:string|null;openedAt:string|null}>;reviewQueue?:Array<{issue:number;author:string;openedAt:string|null;ageHours:number|null;slaBreached:boolean}>;builders:Array<{issue:number;cycleId?:string|null;repoUrl:string;evidenceUrl:string;wallet:string;status:string}>};
const EMPTY_STATUS:PublicStatus={state:"loading",checkedAt:null,metrics:{submitted:null,underReview:null,accepted:null,approvalRate:null,completionRate:null,started:null,dropoffs:null,uniqueBuilders:null,distinctProjects:null,repeatBuilders:null,repeatProjects:null,acceptedPerBuilder:null,averageReviewHours:null,medianReviewHours:null,p90ReviewHours:null,feedbackCount:null,privateFeedbackRequests:null,usefulRate:null,appliedRate:null,repeatIntentRate:null,willingnessToPay:null},builders:[]};
export function usePublicStatus(){const [status,setStatus]=useState<PublicStatus>(EMPTY_STATUS);useEffect(()=>{const controller=new AbortController();void fetch("/api/status",{signal:controller.signal}).then(async response=>(await response.json().catch(()=>null)) as PublicStatus|null).then(value=>{if(value?.state)setStatus(value);else setStatus(current=>({...current,state:"unavailable"}));}).catch((error:unknown)=>{if(error instanceof DOMException&&error.name==="AbortError")return;setStatus(current=>({...current,state:"unavailable"}));});return()=>controller.abort();},[]);return status;}
