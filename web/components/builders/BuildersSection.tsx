"use client";
import { ArrowUpRight,Check } from "lucide-react";
import type { PublicStatus } from "@/hooks/usePublicStatus";
type Props={status:PublicStatus};
const metric=(value:number|null,suffix="")=>value===null?"—":`${value}${suffix}`;
export default function BuildersSection({status}:Props){const degraded=status.state==="partial"||status.state==="unavailable";return <section className="builders-section section-shell" id="builders"><div className="section-heading compact-heading"><div className="section-kicker">HISTÓRICO PÚBLICO</div><h2>Builders aceitos.</h2><p>Métricas derivadas somente de Issues públicas e estados reais do pipeline.</p></div>{status.state==="loading"&&<p className="metrics-health" role="status">Carregando métricas públicas…</p>}{degraded&&<p className="metrics-health metrics-health-warning" role="status">{status.state==="unavailable"?"Métricas temporariamente indisponíveis. Nenhum valor foi substituído por zero.":"Parte das métricas está temporariamente indisponível; valores ausentes aparecem como —."}</p>}<div className="metrics-grid" aria-label="Métricas públicas do ALPHA Builders"><article className="metric-card"><small>SUBMISSÕES</small><strong>{metric(status.metrics.submitted)}</strong></article><article className="metric-card"><small>EM REVISÃO</small><strong>{metric(status.metrics.underReview)}</strong></article><article className="metric-card"><small>ACEITAS</small><strong>{metric(status.metrics.accepted)}</strong></article><article className="metric-card"><small>TAXA DE ACEITE</small><strong>{metric(status.metrics.approvalRate,"%")}</strong></article><article className="metric-card"><small>CONCLUSÃO</small><strong>{metric(status.metrics.completionRate,"%")}</strong></article><article className="metric-card"><small>REVISÃO MÉDIA</small><strong>{metric(status.metrics.averageReviewHours,"h")}</strong></article><article className="metric-card"><small>BUILDERS ÚNICOS</small><strong>{metric(status.metrics.uniqueBuilders)}</strong></article><article className="metric-card"><small>PROJETOS</small><strong>{metric(status.metrics.distinctProjects)}</strong></article><article className="metric-card"><small>BUILDERS RECORRENTES</small><strong>{metric(status.metrics.repeatBuilders)}</strong></article><article className="metric-card"><small>PROJETOS RECORRENTES</small><strong>{metric(status.metrics.repeatProjects)}</strong></article><article className="metric-card"><small>ACEITES / BUILDER</small><strong>{metric(status.metrics.acceptedPerBuilder)}</strong></article></div>{status.checkedAt&&<p className="metrics-timestamp">Última consulta pública: {new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(status.checkedAt))}</p>}{status.state!=="unavailable"&&status.builders.length===0?<div className="empty-builders"><span>0</span><p>Nenhuma contribuição aceita ainda. O histórico começa quando houver evidência real.</p></div>:<div className="builders-grid">{status.builders.map(builder=><article className="builder-card" key={builder.issue}><span className="accepted-pill"><Check size={14}/> Accepted</span><h3>Issue #{builder.issue}</h3><code>{builder.wallet}</code><div className="builder-card-actions"><a href={`/builders/${builder.issue}`}>Perfil público <ArrowUpRight size={15}/></a><a href={builder.evidenceUrl} target="_blank" rel="noreferrer">Evidência <ArrowUpRight size={15}/></a></div></article>)}</div>}</section>}
